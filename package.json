{
  "name": "webhook-outputter",
  "description": "Write webhook reponses to console and have a public adress using localtunnel",
  "version": "1.0.0",
  "scripts": {
    "tunnel": "npx localtunnel --port 8000",
    "start": "npx nodemon index.js"
  },
  "dependencies": {
    "koa": "^2.11.0",
    "koa-bodyparser": "^4.2.1",
    "koa-logger": "^3.2.1",
    "koa-router": "^7.4.0"
  }
}
